<h1 class="job-category-name"><%= @category.name %> Jobs</h1>

<%= render 'layouts/jobs_index' %>




<!-- <div class="container">
  <div class="jobs-list row">
    <div class="col-md-6 col-md-offset-3">

      <h1><%= @category.name %> thingers</h1>

        <% @jobs.each do |job| %>
          <div class="jobs-individual">
            <h3> <%= link_to job.title, business_job_path(business: job.user.slug, id: job.id) %> </h3>
            <p> <%= job.description %> </p>
          </div>
        <% end %>

    </div>
  </div>
</div> -->
