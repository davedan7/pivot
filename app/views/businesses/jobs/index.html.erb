<div class="index-container">
<h2> <%= @business.username.capitalize %> Jobs List</h2>

  <% @jobs.each do |job| %>
    <div class="col-lg-3 col-md-4 col-sm-6">
      <!-- <%= image_tag User.find(job.user_id).picture.url(:thumb) %> -->
      <%= job.title %>
      <%= job.description %>

    </div>
  <% end %>

</div>
