<div class="container">

<h2>All Jobs</h2>

  <% @jobs.each do |job| %>
    <div class="col-lg-3 col-md-4 col-sm-6">
      <h4>
        <%= link_to "#{job.title}",job_path(job) %>
      </h4>
    </div>
  <% end %>

</div>
